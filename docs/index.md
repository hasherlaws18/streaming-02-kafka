# Streaming Data

This site provides documentation for this project.
Use the navigation to explore module-specific materials.

## How-To Guide

Many instructions are common to all our projects.

See
[⭐ **Workflow: Apply Example**](https://denisecase.github.io/pro-analytics-02/workflow-b-apply-example-project/)
to get these projects running on your machine.

## Project Documentation Pages (docs/)

- **Home** - this documentation landing page
- **Project Instructions** - instructions specific to this module
- **Your Files** - how to copy from examples and make them yours
- **Glossary** - project terms and concepts
- **API** - autogenerated look at the code interface

## Custom Project

### Dataset

Describe the dataset used by your Kafka producer.

Include:

- the name of the dataset file
  sales_Asher.csv

- what kind of records it contains
  Sales transaction records with three added rows.

- which fields are included in each record
  currency_code, customer_id, customer_note, datetime,
  device_type, discount_code, is_new_customer,
  is_online, order_id, payment_method, product_id,
  quantity, referral_source, region_id, unit_price

- whether you used the original sales dataset or modified it
  I modified the original sales dataset by adding
  three new sales records and a custom status field
  in the consumer outpu

### Kafka Messages

Describe the messages sent through Kafka.

Include:

- what your producer sends
  My producer sends sales transaction records from
  sales_Asher.csv to a Kafka topic one message at a time.

- which Kafka topic you used
  I used the Kafka topic defined in the .env settings.

- what message key you used, if any
  I used the region_id field as the Kafka message key.

- whether you changed the message fields
  Yes. I added a custom status field with the
  value "processed" to consumed sales messages.

### Consumer Processing

Describe what your consumer receives and does with each message.

Include:

- what your consumer receives from Kafka
  My consumer receives sales transaction messages from the
  Kafka topic product-sales-case.

- how many messages it consumes
  My consumer successfully consumed 12 Kafka messages.

- what it logs or prints
  The consumer logs received messages, processing updates,
  consumed message counts, and a final summary.

- if it writes records to a CSV file
  Yes. The consumer writes processed records to
  consumed_sales_Asher2.csv.

- if it processes or filters selected fields (be specific)
  Yes. The consumer adds a custom status field with the
  value "processed" before writing records to the CSV file.

### Experiments

Describe the small technical changes you made.

Include at least one Phase 4 change and one Phase 5 application.

### Results

Describe what happened when you ran the producer and consumer.

### Interpretation

Explain what the Kafka streaming workflow showed you.

Include:

- what changed from the original example
  I created a custom dataset named sales_Asher.csv,
  added three new sales records, and added a custom
  status field to the consumer output.

- what you learned from watching messages move through Kafka
  I learned how producers send messages to Kafka topics
  and how consumers process messages in real time.

- what the stream could tell a business or organization
  The stream could help monitor sales activity,
  customer behavior, and product demand.

- what business intelligence was gained from the consumed messages
  The data showed product popularity, regional sales trends,
  payment methods, and customer purchasing behavior.
